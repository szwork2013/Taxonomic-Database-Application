# DataSource settings: set here your own configurations for the database 
# connection. In this example we have "netgloo_blog" as database name and 
# "root" as username and password.
spring.jpa.database=POSTGRESQL
spring.database.driverClassName=org.postgresql.Driver
spring.datasource.platform=postgres
spring.datasource.url = jdbc:postgresql://localhost:5432/taxonomic?autoreconnect=true
spring.datasource.username = postgres
spring.datasource.password = ppZCgT5j6_^NnZCh
#spring.datasource.password = )fmK?jPeWhq6KMwm // PROD
#spring.datasource.password = postgres // DEV

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy
# secret token generated by http://nux.net/secret
token.secret=e09a090e9b663599ac0363cd3bca03b29cb368cf
# port configuration
server.port=${port:8080}

# ===============================
# = SOLAR CONFIG
# ===============================
solr.server.url=http://localhost:8983/solr/
solr.solr.home=/var/solr/indexes

# ===============================
# = SPRING BATCH
# ===============================
spring.batch.job.enabled=true
spring.batch.initializer.enabled=false